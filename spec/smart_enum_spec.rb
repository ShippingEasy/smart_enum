require 'spec_helper'

describe SmartEnum do
  it 'has a version number' do
    expect(SmartEnum::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
